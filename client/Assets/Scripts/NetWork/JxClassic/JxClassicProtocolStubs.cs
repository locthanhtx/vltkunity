using System.Reflection;
using System.Threading.Tasks;

namespace game.network.jx
{
    public sealed partial class JxClassicClient
    {
        private Task DispatchNotPortedS2CAsync(JxS2CProtocol protocol, byte[] packet)
        {
            MethodInfo method = GetType().GetMethod(
                "Handle_" + protocol,
                BindingFlags.Instance | BindingFlags.NonPublic);

            return method?.Invoke(this, new object[] { packet }) as Task ?? Task.CompletedTask;
        }

        private Task Handle_s2c_rolehjdkalritopq_getroleinfo_result(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncogjfkdnvhf_roleinfo_cipher(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gamestatistic_bigpackage(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_micfghjkdtropackbegin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_accountbegin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_accountfdfsglogin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gamelogin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_accountlogout(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gatewayverify(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gatewayverifyagain(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gatewayinfo(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_multiserverbegin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_querymapinfo(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_querygameserverinfo(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_identitymapping(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_notifypl_ayerlogin_hfkqotivbhd(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_notifyplayerexchange(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_notifysvrip(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_roleserver_getrolelist_result(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_roleserver_saverole_result(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_roleserver_createrole_result(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_roleserver_deleterole_result(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_logiclogout(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gateway_broadcast(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gamestatistic(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_rolecheckname_result(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gwsyn_broadcast(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_clientbegin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_login(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_logout(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncclientend(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_synonlineplayer(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_synccurplayerskill(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_synccurplayernormal(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_newplayer(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_removeplayer(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncworld(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncplayer(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncplayermin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncnpc(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncnpcmin(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncnpcminplayer(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_objadd(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncobjstate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncobjdir(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_objremove(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_objTrapAct(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcremove(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcwalk(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcrun(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcattack(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcmagic(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcjump(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npctalk(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npchurt(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcdeath(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcchgcurcamp(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcchgcamp(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcsettmpcamp(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_skillcast(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playertalk(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playerexp(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teaminfo(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamselfinfo(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamapplyinfofalse(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamcreatesuccess(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamcreatefalse(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamopenclose(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamgetapply(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamaddmember(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamleave(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teamchangecaptain(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playerfactiondata(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playerleavefaction(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playerfactionskilllevel(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playersendchat(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playersyncleadexp(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playerlevelup(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teammatelevel(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playersyncattribute(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playerskilllevel(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncitem(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_removeitem(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncmonfs(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncqianfs(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playermoveitem(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_scriptaction(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatapplyaddfriend(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chataddfriend(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatrefusefriend(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chataddfriendfail(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatloginfriendnoname(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatloginfriendname(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatonefrienddata(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatfriendonline(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatdeletefriend(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatfriendoffline(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncrolelist(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_tradechangestate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcsetmenustate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_trademoneysync(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_tradedecision(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chatscreensingleerror(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncnpcstate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_teaminviteadd(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_tradepressoksync(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_ping(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcsit(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcstand(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_opensalebox(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_castskilldirectly(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_msgshow(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncstateeffect(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openstorebox(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openresetpass(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openstringbox(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_opendataubox(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playerrevive(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_requestnpcfail(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_tradeapplystart(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_rolenewdelresponse(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_ItemAutoMove(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_itemexchangefinish(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_changeweather(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_pksyncnormalflag(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_pksyncenmitystate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_pksyncexercisestate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_pksyncpkvalue(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_reputesyncreputevalue(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_fuyuansyncfuyuanvalue(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_rebornsyncrebornvalue(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openmarketbox(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcsleepmode(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_viewequip(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_ladderresult(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_ladderlist(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_tongcreate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_replyclientping(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcgoldchange(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_itemdurabilitychange(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_opentremble(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_rankname(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_viewsellitem(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_viewupdateitem(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playergetcount(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_shopname(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncmasklock(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_opengive(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcsecmove(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_pkvalue(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_viewlianitem(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openwengang(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openstone(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_updateviewequip(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gettongback(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncequipitem(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_taskcallback(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openjindutiao(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncitemposition(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openduanzhao(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncurupdata(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_JinMaicallback(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_openvipdazao(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_synnpcinfocallback(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_pautoMovecallback(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_plyerguzicallback(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncplayermap(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_synconestate(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncnodataeffect(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_npcsetpos(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_playermissiondata(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncrankdata(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncsupershop(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_syncitem_shop(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_itemdata_shop(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_kickoutgame(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_objadd_vn(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_rolesyncvipvalue(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_gm_synroleaction(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_queryiteminfo(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_returnitemlock(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_findpath_state(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_refitemsync(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_chestpwsync(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_skill_prop_point_sync(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_extend(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_extendchat(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_extendfriend(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_extendchat_new(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_extendtong(byte[] packet) => Task.CompletedTask;
        private Task Handle_s2c_end(byte[] packet) => Task.CompletedTask;
    }
}
